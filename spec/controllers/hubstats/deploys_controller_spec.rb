require 'spec_helper'

module Hubstats
  RSpec.describe DeploysController, :type => :controller do
  	#describe "#create" do
  #		it 'creates a deploy using controller method' do
   #   	deploy_hash = {
    #  		git_revision: 193, 
    #  		repo_id: "sportngin/ngin", 
    #  		deployed_at: "2009-02-03 03:00:00 -0500", 
    #  		deployed_by: "emmasax1"
    #  	}

     # 	get :index
      
      	#@deploy = DeploysController.create(deploy_hash)
      #	expect(response).to have_http_status(200)

      	#expect(deploy.git_revision).to eq(deploy_hash[:git_revision])
      	#expect(deploy.repo_id).to eq(126422)
      	#expect(deploy.deployed_at).to eq(deploy_hash[:deployed_at])
      	#expect(deploy.deployed_by).to eq(deploy_hash[:deployed_by])
    	#end
    #end
  end
end
