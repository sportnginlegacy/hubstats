<% if @deploys.length > 0 %>
  <div class="deploys">
    <%= render partial: 'hubstats/partials/deploy-condensed', collection: @deploys, as: :deploy %>
  </div>
<% else %>
  <p> No deploys to show. Even though there are actually <%= @deploy_count%> deploys </p>
<% end %>
